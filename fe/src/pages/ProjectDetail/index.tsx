import { Flex } from 'antd'
import styled from 'styled-components'

const Wrapper = styled(Flex)``
const ProjectDetail = () => {
  return <Wrapper>ProjectDetail</Wrapper>
}

export default ProjectDetail
