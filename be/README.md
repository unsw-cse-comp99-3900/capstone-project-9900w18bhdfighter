1. Created and activate a virtual environment in the be file(myenv)
2. Install and set up Django project(myproject)         (use pip3/python3 rather than pip)


Django version 5.0.6, using settings 'myproject.settings'
Starting development server at http://127.0.0.1:8000/